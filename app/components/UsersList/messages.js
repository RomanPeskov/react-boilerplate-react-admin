import { defineMessages } from 'react-intl';

export const scope = 'components.usersList';

export default defineMessages({
  name: { id: `${scope}.name` },
  notFound: { id: `${scope}.notFound` },
  readMore: { id: `${scope}.readMore` },
});
