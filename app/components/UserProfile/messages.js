import { defineMessages } from 'react-intl';

export const scope = 'components.userProfile';

export default defineMessages({
  email: { id: `${scope}.email` },
  name: { id: `${scope}.name` },
  goBack: { id: `${scope}.goBack` },
  notFound: { id: `${scope}.notFound` },
  saveProfile: { id: `${scope}.saveProfile` },
  editProfile: { id: `${scope}.editProfile` },
});
